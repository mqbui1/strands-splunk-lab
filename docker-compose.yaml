version: "3.9"

services:
  splunk-otel-collector:
    image: ghcr.io/signalfx/splunk-otel-collector:0.144.0
    container_name: splunk-otel-collector
    ports:
      - "4317:4317" # OTLP gRPC (traces)
      - "4318:4318" # OTLP HTTP (metrics/logs)
    volumes:
      - ./otel-config.yaml:/etc/otel/config.yaml:ro
    command: ["--config", "/etc/otel/config.yaml"]

  strands-agent:
    build: .
    container_name: strands-agent
    depends_on:
      - splunk-otel-collector
    environment:
      STRANDS_MODEL: "local"  # just a marker
